[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "https://your-backend-url.com/:splat"
  status = 200
  force = true

# For SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200